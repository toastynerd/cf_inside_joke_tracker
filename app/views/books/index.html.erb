<div class="panel large-10 column">
  <%= link_to "In the know peeps", in_the_know_peeps_path, :class => "button expand" %>
  <h2>Books:</h2>
  <ul>
    <% @books.each do |book| %>
      <li><%= link_to book.name, book %></li>
    <% end %>
</ul>

  <%= link_to "Create a New Book", new_book_path %>
</div>