<%= form_for [@book,@joke] do |f| %>
  <%= f.error_messages %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :meaning %>
  <%= f.text_field :meaning %>

  <%= f.submit :class => "button" %>
<% end %>